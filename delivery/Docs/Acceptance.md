# 发布验收记录（待执行，不是通过证明）

填写：SDK 完整版本、Demo 完整版本、Xcode/Swift 版本、验证人、日期、设备/系统。

| 检查项 | 状态 |
| --- | --- |
| iOS arm64 与模拟器切片存在，含 Swift interface | 待验证 |
| 主库及所有非系统动态依赖齐备，静态/动态类型注明 | 待验证 |
| 消费工程无需 SDK 源码即可 import、编译、链接 | 待验证 |
| Swift interface 依赖的第三方模块齐备 | 待验证 |
| JS 桥接、Metal 和第三方资源在安装包内可加载 | 待验证 |
| 真机冷启动、首次播放、音画、切地址 | 待验证 |
| 暂停/继续/静音、播放进度、错误提示 | 待验证 |
| 非法地址、404、断网、超时、恢复策略 | 待验证 |
| 连续进入退出与快速切换，没有残留音频 | 待验证 |
| 日志 version 与 SDK 完整版本及 manifest 一致 | 待验证 |
| 签名/安装、系统版本与格式支持矩阵 | 待验证 |
| SDK 分发授权和第三方许可证、源代码义务等已确认 | 待确认 |

提供 HLS/H.264、HLS/H.265、HTTP-FLV、RTMP 的实际测试结果；未测试或不支持项目明确标注，不因 API 暴露就写成支持。

打包脚本复制依赖仓库中找到的许可证，仅用于准备材料，不代表完整许可清单或合规结论。主 SDK 仓库若没有 LICENSE，需要所有者明确业务分发授权；不能编造许可证。

候选包未通过以上项目之前，不移除 CANDIDATE 标记、不标记正式验收。正式交付应保留校验和及不可变版本记录。
